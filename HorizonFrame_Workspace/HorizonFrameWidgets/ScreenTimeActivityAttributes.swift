// import ActivityKit
import SwiftUI // Adding SwiftUI as it's often used with ActivityAttributes for previews or simple views

/*
// Define ScreenTimeActivityAttributes in its own file
// This struct defines the dynamic data for the Live Activity.
struct ScreenTimeActivityAttributes: ActivityAttributes {
    public struct ContentState: Codable, Hashable {
        // Dynamic values that update frequently
        var elapsedHours: Double
        var insight: String // Example: "Keep going!", "Take a break soon."
    }

    // Static values that don't change for the lifetime of the activity
    var dailyGoal: Double // Example: Target hours for screen time
    // You could add other static properties like activityName, etc.
}
*/
